<header class="bg-center h-48 relative overflow-hidden">
  <!--
    bg-center: 背景画像を中央に配置
    relative: 子要素の位置決めの基準点となる
    overflow-hidden: はみ出た内容を隠す
  -->
  <div class="absolute inset-0 bg-center bg-cover"
    style="background-image: url('<%= asset_path('Designer.png') %>')">
    <!--
      absolute inset-0: 親要素いっぱいに広げる
      bg-center: 背景画像を中央に配置
      bg-cover: 背景画像を要素全体に広げ、画像が歪まないようにサイズを調整する
    -->
  </div>
  <div class="absolute inset-0 bg-black bg-opacity-50"></div>
  <!-- bg-opacity-50: 背景を50%の透明度にする -->
  <div class="relative z-10 h-full flex items-center justify-center px-4">
    <!-- z-10: 重なり順を指定　明示的に指定が他にないので、他のz-indexは0となる -->
    <!-- items-center justify-center: 子要素を中央に配置 -->
    <h1 class="text-3xl font-bold text-white text-center">BabyGourmet</h1>
  </div>
</header>