<div class="max-w-4xl mx-auto bg-base-100 shadow-lg rounded-lg overflow-hidden">
  <div class="bg-gray-50 p-6 border-b">
    <div class="flex items-center space-x-4">
      <%= image_tag @user.avatar.url, class: 'h-20 w-20 rounded-full border-4 border-white shadow-md' %>
      <div>
        <h2 class="text-xl font-bold text-gray-800"><%= @user.name %></h2>
        <p class="text-sm text-gray-600"><%= @user.recipes.count %> <%= t('users.profile.recipe_count') %></p>
      </div>
    </div>
  </div>

  <div class="mx-auto px-4">
    <div class="flex justify-center items-center my-8">
      <h1 class="relative inline-flex items-center text-4xl font-bold px-16 py-2">
        <span class="absolute left-0 w-12 h-[1px] bg-black"></span>
        <div class="text-xl sm:text-2xl font-bold text-gray-800"><%= @user.name %>さんのレシピ集</div>
        <span class="absolute right-0 w-12 h-[1px] bg-black"></span>
      </h1>
    </div>
      <!--
        flex justify-center items-center :水平・垂直方向に中央寄せ
        relative :相対配置、before,afterの絶対配置の基準となる
        inline-block :インラインブロック要素、横並びにする
        before:content-[''] :before要素に空のコンテンツを設定
        before:absolute :before要素を絶対配置
        before:top-1/2 :before要素を縦方向中央に配置
        before:left-0 :before要素を横方向左端に配置
        before:inline-block :before要素をインラインブロック要素にする
      -->
    <% if @user.recipes.exists? %>
      <div class="flex justify-center items-center my-8">
        <%= render 'shared/user_recipe_list', recipes: @recipes, user: @user %>
      </div>
    <% else %>
      <p class="text-gray-600 text-center py-8">
        <%= @user.name %>さんは、まだレシピを投稿していません。
      </p>
    <% end %>
  </div>
</div>
